<resources>
    <style name="Theme.Sacram" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorSecondary">@color/accent</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:colorAccent">@color/accent</item>

        <!-- Brand-colored controls: inputs, checkboxes, focus states -->
        <item name="colorControlNormal">@color/surface_line</item>
        <item name="colorControlActivated">@color/accent</item>
        <item name="android:textColorHint">#6B7589</item>

        <!-- Outlined text fields get the accent stroke + rounded corners -->
        <item name="textInputStyle">@style/Widget.Sacram.TextInputLayout</item>

        <item name="android:windowBackground">@color/bg</item>
        <item name="android:colorBackground">@color/bg</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:buttonCornerRadius">14dp</item>
    </style>

    <style name="Widget.Sacram.TextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/accent</item>
        <item name="hintTextColor">@color/accent</item>
        <item name="boxStrokeWidthFocused">2dp</item>
        <item name="android:textColorHint">#6B7589</item>
    </style>
</resources>
